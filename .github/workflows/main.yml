on:
  push:
    branches:
      - dev
name: 🚀 Deploy SFA Udaan on push on dev branch
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: v4account.com
        username: sfa_udaan
        password: !#AWcT49yi0rQmJg
        dry-run: true
